#2 Check if election logic is working.
#4 Unit tests.
#5 Document the options and the code. add typespecs.


Testing
1.1 Config handler still needs sync start test and delay tests.
1.2 Use peer module to spawn child nodes and test rpc client.
1.3 Use peer module to spawn child nodes and test runtime strategies.