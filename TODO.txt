#2 Check if election logic is working.
#4 Unit tests.
#5 Document the options and the code. add typespecs.


Testing
1.1 Test Elector
1.2 Config handler still needs sync start test and delay tests.
1.3 Runtime startegy should also check if nodes are having the same runtime and compare node names in that case as well.