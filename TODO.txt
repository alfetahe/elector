1. heck of everything working well with the app module.
2.Check the current implementation from the sd streamer and port it here.
4. Add configuration options(document them as well) to start sync/async, define the strategy module, election interval, leader slection callback, election delay etc